# server/ — Sprint 3

API `node:http` (sans framework) + service des fichiers statiques de
`dist/`. Zéro egress. Persistance SQLite, journal `card_events` append-only.
Rien n'est implémenté avant le Sprint 3.

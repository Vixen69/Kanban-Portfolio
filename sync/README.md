# sync/ — Sprint 5

Processus CLI séparé : tire depuis l'adaptateur actif (csv-import, puis
sciforma en lecture seule), écrit dans SQLite, se termine. Jamais intégré
au serveur web. Identifiants dans un fichier hors dépôt (chmod 600),
référencé par chemin. Rien n'est implémenté avant le Sprint 5.
